
server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Javs8356/NTTDataBootcamp_ConfigServer.git
        default-label: main
        search-paths:
        - '*service*'